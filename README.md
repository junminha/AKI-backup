# AKI backup

Private source snapshot of `C:\Users\user\Desktop\AKI`.

The backup includes project source files and runtime assets. Nested Git metadata,
dependency folders, generated build output, local `.env` files, certificates,
and logs are intentionally excluded. Environment variable templates such as
`.env.example` remain included.
